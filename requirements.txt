# Runtime deps for the RQ1–RQ3 portfolio-allocation pipelines.
# Install with:  pip install -r requirements.txt && pip install -e . --no-deps
numpy>=1.17.3
pandas>=1.1.5
matplotlib
scipy
scikit-learn>=0.21
gymnasium
torch
tensorboard
stable-baselines3[extra]>=2.0.0a5
pyfolio-reloaded>=0.9
yfinance>=0.2
pyportfolioopt>=1
